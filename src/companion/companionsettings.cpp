#include "companion/companionsettings.h"

// CompanionSettings is a header-only inline wrapper over UserSettings. This
// translation unit exists so the class has a stable compilation home and to
// match the source list in CMakeLists.txt; add out-of-line members here if the
// settings surface grows beyond simple accessors.
